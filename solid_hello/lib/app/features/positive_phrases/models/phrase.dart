/// Represents a phrase with its author.
class Phrase {
  /// The phrase text.
  final String text;

  /// The author of the phrase.
  final String author;

  /// Creates a Phrase instance with the given text and author.
  const Phrase(this.text, this.author);
}
